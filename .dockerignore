# Keep the Docker build context lean and unambiguous. The test project must
# not enter the image build (a bare `dotnet publish` over multiple projects
# fails); the Dockerfile also names MailArchiver.csproj explicitly.
**/bin/
**/obj/
tests/
.git/
.github/
.claude/
Screenshots/
docker-compose.yml
postgres-data/
**/DataProtection-Keys/
