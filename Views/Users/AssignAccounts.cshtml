@model MailArchiver.Models.ViewModels.UserMailAccountViewModel

@{
    ViewData["Title"] = "Assign Mail Accounts";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1>Assign Mail Accounts to User</h1>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Back to List
        </a>
    </div>

    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0">User: @Model.User.Username</h5>
        </div>
        <div class="card-body">
            <form asp-action="AssignAccounts">
                <input type="hidden" asp-for="User.Id" />
                
                <div class="mb-3">
                    <label class="form-label">Available Mail Accounts</label>
                    <div class="card">
                        <div class="card-body">
                            @if (Model.AllMailAccounts.Any())
                            {
                                <div class="row">
                                    @foreach (var account in Model.AllMailAccounts)
                                    {
                                        <div class="col-md-6 mb-2">
                                            <div class="form-check">
                                                <input class="form-check-input" 
                                                       type="checkbox" 
                                                       id="account_@account.Id" 
                                                       name="SelectedAccountIds" 
                                                       value="@account.Id"
                                                       @(Model.AssignedAccountIds.Contains(account.Id) ? "checked" : "")>
                                                <label class="form-check-label" for="account_@account.Id">
                                                    <strong>@account.Name</strong> (@account.EmailAddress)
                                                    @if (account.IsEnabled)
                                                    {
                                                        <span class="badge bg-success ms-2">Enabled</span>
                                                    }
                                                    else
                                                    {
                                                        <span class="badge bg-danger ms-2">Disabled</span>
                                                    }
                                                </label>
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                            else
                            {
                                <p class="text-muted">No mail accounts available.</p>
                            }
                        </div>
                    </div>
                </div>
                
                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Save Assignments
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
